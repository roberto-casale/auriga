# Runtime del gioco
pygame==2.6.1

# Solo per gli strumenti di build asset in tools/ (musica, ritagli)
numpy>=1.26
pillow>=10

# Facoltativi (gia' presenti nel venv di test_py): torch + diffusers,
# usati solo da tools/generate_portraits_sd.py per rigenerare i ritratti.
